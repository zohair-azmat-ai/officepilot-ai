# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
venv/
.venv/
*.egg-info/
dist/
build/
*.egg

# ── Environment / Secrets ─────────────────────────────────────────────────────
.env
# (use .env.example as the committed template)

# ── User-generated output ─────────────────────────────────────────────────────
output/
# Ledger data is user-specific; seed files (empty []) stay tracked
data/invoices.json
data/payments.json

# ── Desktop app build artifacts ───────────────────────────────────────────────
desktop-app/node_modules/
desktop-app/dist/

# ── Excel template (generated at runtime / Docker build — not committed) ─────
# Run `python create_template.py` locally to generate this file.
templates/quotation_template.xlsx

# ── Windows-specific launchers (contain machine-specific paths) ───────────────
start_backend.vbs

# ── Editor / OS ───────────────────────────────────────────────────────────────
.vscode/
.idea/
*.suo
*.user
Thumbs.db
.DS_Store
ehthumbs.db
Desktop.ini
